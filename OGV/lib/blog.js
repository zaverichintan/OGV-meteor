Blog.config({
  comments: {
    useSideComments: true, // default is false
    allowAnonymous: true   // default is false
  }
});
Blog.config({
  rss: {
    title: 'BRL CAD Online Geometry viewer',
    description: 'Updates on BRLCAD Online Geometry Viewer '
  }
});
Blog.config({
  pageSize: 10
});
